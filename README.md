# Biome React Rules

This repo is a specification suite for an agentic loop to satisfy. It takes
all applicable test cases from https://github.com/jsx-eslint/eslint-plugin-react,
converts them into a harness suite that the agentic loop relies on
and asserts results across cases.